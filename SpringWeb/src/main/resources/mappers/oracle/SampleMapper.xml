<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="common.mapper.SampleMapper"><!-- SampleMapper ==> interface  -->
	<select id="testCnt" resultType="_int"> <!-- id="testCnt" ==> 메서드이름 -->
		select count(*) from tab
	</select>
	
	<select id="getTableInfo" resultType="svo">
		select * from tab
	</select>
</mapper>
